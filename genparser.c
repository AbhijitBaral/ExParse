#include "ExParse.h"

double parse(char *input){
   //Does something and writes the expression into the parser file containing a single function that returns the evaluated result. This result is then returned to the caller function by this "parse" function


   return(eval);
} 
